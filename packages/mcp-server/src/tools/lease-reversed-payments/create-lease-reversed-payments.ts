// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.

import { Tool } from '@modelcontextprotocol/sdk/types.js';
import type { Metadata } from '../';
import Doorloop from 'doorloop';

export const metadata: Metadata = {
  resource: 'lease_reversed_payments',
  operation: 'write',
  tags: [],
};

export const tool: Tool = {
  name: 'create_lease_reversed_payments',
  description: 'Create a Lease Returned Payment which marks an existing Lease Payment as returned',
  inputSchema: {
    type: 'object',
    properties: {
      date: {
        type: 'string',
        description: 'Format: YYYY-MM-DD',
      },
      lease: {
        type: 'string',
        title: 'mongoId',
      },
      leasePayment: {
        type: 'string',
        description: 'Referenced the original Lease Payment Id that was returned',
      },
      lines: {
        type: 'array',
        items: {
          type: 'object',
          title: 'leaseTransactionLine',
          properties: {
            account: {
              type: 'string',
              description: 'References the Account Id for this line.',
            },
            amount: {
              type: 'number',
            },
            id: {
              type: 'string',
              title: 'mongoId',
            },
            balance: {
              type: 'string',
              description: 'Read Only.',
            },
            memo: {
              type: 'string',
            },
          },
          required: ['account', 'amount'],
        },
      },
      batch: {
        type: 'string',
      },
      memo: {
        type: 'string',
      },
      reference: {
        type: 'string',
        description: 'If not provided one will be generated by the server',
      },
    },
  },
};

export const handler = (client: Doorloop, args: Record<string, unknown> | undefined) => {
  const body = args as any;
  return client.leaseReversedPayments.create(body);
};

export default { metadata, tool, handler };
